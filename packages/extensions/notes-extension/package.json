{
  "name": "xplorer-notes",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "xplorer": {
    "id": "notes",
    "name": "Notes",
    "displayName": "File Notes",
    "description": "Add and manage notes for files and folders",
    "version": "1.0.0",
    "author": "Xplorer",
    "category": "panel",
    "icon": "sticky-note",
    "permissions": ["storage", "ui:panels"]
  },
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --outfile=dist/index.js --format=iife --jsx=transform --target=es2020 --external:react --external:react-dom --external:@xplorer/extension-sdk",
    "dev": "touch .hotreload && esbuild src/index.tsx --bundle --outfile=dist/index.js --watch --format=iife --external:react --external:react-dom --external:@xplorer/extension-sdk --define:process.env.NODE_ENV=\\\"development\\\"; rm -f .hotreload"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
