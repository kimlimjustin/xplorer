{
  "name": "xplorer-storage-analytics",
  "version": "1.0.0",
  "description": "Analyze storage usage and file distribution",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --format=iife --outfile=dist/index.js --external:react --external:react-dom --external:@xplorer/extension-sdk --target=es2020 --jsx=transform"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "@types/react": "^18.2.0",
    "typescript": "^5.2.0"
  },
  "xplorer": {
    "id": "storage-analytics",
    "name": "Storage Analytics",
    "displayName": "Storage Analytics",
    "description": "Analyze storage usage and file distribution",
    "version": "1.0.0",
    "author": "Xplorer Team",
    "category": "panel",
    "icon": "chart",
    "permissions": ["file:read", "ui:panels"],
    "activationEvents": ["onStartup"]
  }
}
