# Database (PostgreSQL — local Docker for dev, Neon for production)
# Local dev: run `docker compose up -d postgres` from project root
DATABASE_URL="postgresql://xplorer:xplorer@localhost:15432/xplorer?schema=public"

# NextAuth
NEXTAUTH_SECRET="generate-a-random-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# GitHub OAuth (for sign-in)
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# GitHub Personal Access Token (for sponsor status checking)
# Create at https://github.com/settings/tokens — needs read:org scope
GITHUB_PAT=""

# Stripe (for paid extension purchases + Connect author payouts)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Vercel Blob (for extension file storage)
BLOB_READ_WRITE_TOKEN=""
