/**
 * TauriAPI facade — re-exports a backward-compatible static class composed
 * from domain-specific modules.  Individual named exports are also available
 * for tree-shaking.
 */

import * as fileSystem from './file-system';
import * as search from './search';
import * as git from './git';
import * as extensions from './extensions';
import * as ai from './ai';
import * as storage from './storage';
import * as system from './system';
import * as pty from './pty';

// Re-export all types so `import { FileEntry } from '@/lib/tauri-api'` keeps working
export * from '../tauri-api-types';

// Re-export every function individually for tree-shaking
export * from './file-system';
export * from './search';
export * from './git';
export * from './extensions';
export * from './ai';
export * from './storage';
export * from './system';
export * from './pty';

// ---------------------------------------------------------------------------
// Backward-compatible static class
// ---------------------------------------------------------------------------

export class TauriAPI {
  // ── File system ─────────────────────────────────────────────────────────
  static readDirectory = fileSystem.readDirectory;
  static getFileProperties = fileSystem.getFileProperties;
  static getFileMetaData = fileSystem.getFileMetaData;
  static getDetailedFileProperties = fileSystem.getDetailedFileProperties;
  static getDirectorySize = fileSystem.getDirectorySize;
  static getDirectoryItemCount = fileSystem.getDirectoryItemCount;
  static setFilePermissions = fileSystem.setFilePermissions;
  static getFileAssociations = fileSystem.getFileAssociations;
  static openFileWithApplication = fileSystem.openFileWithApplication;
  static getSystemApplications = fileSystem.getSystemApplications;
  static setDefaultApplication = fileSystem.setDefaultApplication;
  static compressFiles = fileSystem.compressFiles;
  static getCompressionInfo = fileSystem.getCompressionInfo;
  static extractArchive = fileSystem.extractArchive;
  static getArchiveInfo = fileSystem.getArchiveInfo;
  static extractSelectedEntries = fileSystem.extractSelectedEntries;
  static isArchive = fileSystem.isArchive;
  static encryptFile = fileSystem.encryptFile;
  static decryptFile = fileSystem.decryptFile;
  static isEncryptedFile = fileSystem.isEncryptedFile;
  static secureDelete = fileSystem.secureDelete;
  static copy = fileSystem.copy;
  static moveFile = fileSystem.moveFile;
  static rename = fileSystem.rename;
  static bulkRename = fileSystem.bulkRename;
  static removeDir = fileSystem.removeDir;
  static removeFile = fileSystem.removeFile;
  static moveToTrash = fileSystem.moveToTrash;
  static openRecycleBin = fileSystem.openRecycleBin;
  static getTrashItems = fileSystem.getTrashItems;
  static restoreFromTrash = fileSystem.restoreFromTrash;
  static emptyTrash = fileSystem.emptyTrash;
  static permanentlyDeleteFromTrash = fileSystem.permanentlyDeleteFromTrash;
  static isDir = fileSystem.isDir;
  static getFilesInDirectory = fileSystem.getFilesInDirectory;
  static openFile = fileSystem.openFile;
  static fileExists = fileSystem.fileExists;
  static createDirRecursive = fileSystem.createDirRecursive;
  static createFile = fileSystem.createFile;
  static createFileWithContent = fileSystem.createFileWithContent;
  static createSymlink = fileSystem.createSymlink;
  static getDirSize = fileSystem.getDirSize;
  static readTextFile = fileSystem.readTextFile;
  static readBinaryFile = fileSystem.readBinaryFile;
  static calculateFolderSize = fileSystem.calculateFolderSize;
  static getCachedFolderSizes = fileSystem.getCachedFolderSizes;
  static clearFolderSizeCache = fileSystem.clearFolderSizeCache;
  static undoOperation = fileSystem.undoOperation;
  static redoOperation = fileSystem.redoOperation;
  static getUndoHistory = fileSystem.getUndoHistory;
  static clearUndoHistory = fileSystem.clearUndoHistory;
  static getFileTemplates = fileSystem.getFileTemplates;
  static createFromTemplate = fileSystem.createFromTemplate;
  static getDirectorySizes = fileSystem.getDirectorySizes;
  static checkConflicts = fileSystem.checkConflicts;
  static getRenameDest = fileSystem.getRenameDest;
  static showInFolder = fileSystem.showInFolder;
  static copyWithProgress = fileSystem.copyWithProgress;
  static moveWithProgress = fileSystem.moveWithProgress;
  static acceleratedCopyFile = fileSystem.acceleratedCopyFile;
  static acceleratedCopyDirectory = fileSystem.acceleratedCopyDirectory;
  static listenToFileOperationProgress = fileSystem.listenToFileOperationProgress;
  static computeFileHash = fileSystem.computeFileHash;
  static compareFiles = fileSystem.compareFiles;
  static findDuplicates = fileSystem.findDuplicates;
  static deleteDuplicateFiles = fileSystem.deleteDuplicateFiles;
  static moveDuplicateFilesToTrash = fileSystem.moveDuplicateFilesToTrash;
  static cancelDuplicateScan = fileSystem.cancelDuplicateScan;
  static analyzeDirectory = fileSystem.analyzeDirectory;
  static previewOrganization = fileSystem.previewOrganization;
  static executeOrganization = fileSystem.executeOrganization;
  static addRecentFile = fileSystem.addRecentFile;
  static getRecentFiles = fileSystem.getRecentFiles;
  static clearRecentFiles = fileSystem.clearRecentFiles;
  static removeRecentFile = fileSystem.removeRecentFile;
  static resizeImage = fileSystem.resizeImage;
  static convertImage = fileSystem.convertImage;
  static getImageInfo = fileSystem.getImageInfo;
  static batchProcessImages = fileSystem.batchProcessImages;
  static rotateImage = fileSystem.rotateImage;
  static flipImage = fileSystem.flipImage;
  static cropImage = fileSystem.cropImage;
  static adjustBrightness = fileSystem.adjustBrightness;
  static adjustContrast = fileSystem.adjustContrast;
  static grayscaleImage = fileSystem.grayscaleImage;
  static startWatching = fileSystem.startWatching;
  static stopWatching = fileSystem.stopWatching;
  static watchDirectory = fileSystem.watchDirectory;
  static unwatchDirectory = fileSystem.unwatchDirectory;
  static getActiveWatchers = fileSystem.getActiveWatchers;
  static showOpenDialog = fileSystem.showOpenDialog;

  // ── Search ──────────────────────────────────────────────────────────────
  static findFiles = search.findFiles;
  static searchInFiles = search.searchInFiles;
  static setTokenizerSettings = search.setTokenizerSettings;
  static getTokenizerSettings = search.getTokenizerSettings;
  static rebuildTokenIndex = search.rebuildTokenIndex;
  static searchTokens = search.searchTokens;
  static naturalLanguageSearch = search.naturalLanguageSearch;
  static getTokenizerStats = search.getTokenizerStats;
  static isTokenizerIndexing = search.isTokenizerIndexing;
  static getFileTokens = search.getFileTokens;
  static addPathToTokenizer = search.addPathToTokenizer;
  static getFileRecommendations = search.getFileRecommendations;
  static indexDirectory = search.indexDirectory;
  static setSearchContext = search.setSearchContext;
  static addWhitelistedPath = search.addWhitelistedPath;
  static aiSearch = search.aiSearch;
  static parseSearchQuery = search.parseSearchQuery;
  static enhancedSearch = search.enhancedSearch;
  static getAIIndexStatus = search.getAIIndexStatus;
  static triggerAIIndexing = search.triggerAIIndexing;
  static getAIIndexEntry = search.getAIIndexEntry;
  static semanticSearch = search.semanticSearch;
  static findSimilarFiles = search.findSimilarFiles;

  // ── Git ─────────────────────────────────────────────────────────────────
  static findGitRepository = git.findGitRepository;
  static getRepositoryInfo = git.getRepositoryInfo;
  static getFileHistory = git.getFileHistory;
  static getFileBlame = git.getFileBlame;
  static getFileDiff = git.getFileDiff;
  static getCommitDiff = git.getCommitDiff;
  static getAllCommits = git.getAllCommits;
  static getFileStatus = git.getFileStatus;
  static getBranches = git.getBranches;
  static createBranch = git.createBranch;
  static switchBranch = git.switchBranch;
  static deleteBranch = git.deleteBranch;
  static stageFile = git.stageFile;
  static unstageFile = git.unstageFile;
  static commitChanges = git.commitChanges;
  static getStashes = git.getStashes;
  static createStash = git.createStash;
  static applyStash = git.applyStash;
  static dropStash = git.dropStash;
  static getGitStatus = git.getGitStatus;
  static getGitRepoInfo = git.getGitRepoInfo;

  // ── Extensions ──────────────────────────────────────────────────────────
  static getExtensionStorage = extensions.getExtensionStorage;
  static setExtensionStorage = extensions.setExtensionStorage;
  static deleteExtensionStorage = extensions.deleteExtensionStorage;
  static getInstalledExtensions = extensions.getInstalledExtensions;
  static installExtensionFromPath = extensions.installExtensionFromPath;
  static uninstallExtensionById = extensions.uninstallExtensionById;
  static activateExtension = extensions.activateExtension;
  static deactivateExtension = extensions.deactivateExtension;
  static getActiveExtensionIds = extensions.getActiveExtensionIds;
  static getExtensionPermissions = extensions.getExtensionPermissions;
  static validateExtensionPath = extensions.validateExtensionPath;
  static downloadAndInstallExtension = extensions.downloadAndInstallExtension;
  static packExtension = extensions.packExtension;
  static installXtensionFile = extensions.installXtensionFile;
  static inspectXtensionFile = extensions.inspectXtensionFile;
  static nativePluginInvoke = extensions.nativePluginInvoke;
  static extensionBackendCall = extensions.extensionBackendCall;
  static extensionBackendStatus = extensions.extensionBackendStatus;
  static checkForExtensionUpdates = extensions.checkForExtensionUpdates;
  static downloadExtensionUpdate = extensions.downloadExtensionUpdate;

  // ── AI ──────────────────────────────────────────────────────────────────
  static getAiModels = ai.getAiModels;
  static checkOllamaStatus = ai.checkOllamaStatus;
  static chatWithAI = ai.chatWithAI;
  static analyzeFileWithAI = ai.analyzeFileWithAI;
  static getFileHelp = ai.getFileHelp;
  static suggestFilename = ai.suggestFilename;
  static autoTagFiles = ai.autoTagFiles;
  static agentReadFileTree = ai.agentReadFileTree;
  static agentRequestWritePermission = ai.agentRequestWritePermission;
  static agentWriteFileWithPermission = ai.agentWriteFileWithPermission;
  static agentRespondApproval = ai.agentRespondApproval;
  static agentCancelSession = ai.agentCancelSession;
  static getAgentSettings = ai.getAgentSettings;
  static updateAgentSettings = ai.updateAgentSettings;
  static updateAgentApiKeys = ai.updateAgentApiKeys;

  // ── Storage ─────────────────────────────────────────────────────────────
  static getBookmarks = storage.getBookmarks;
  static addBookmark = storage.addBookmark;
  static removeBookmark = storage.removeBookmark;
  static updateBookmarkName = storage.updateBookmarkName;
  static analyzeStorage = storage.analyzeStorage;
  static diagnoseDirectory = storage.diagnoseDirectory;
  static getFileTags = storage.getFileTags;
  static setFileTags = storage.setFileTags;
  static getAllFileTags = storage.getAllFileTags;
  static getFileTagsBatch = storage.getFileTagsBatch;
  static removeAllTagsFromFile = storage.removeAllTagsFromFile;
  static removeTagGlobally = storage.removeTagGlobally;
  static batchAddTags = storage.batchAddTags;
  static batchRemoveTags = storage.batchRemoveTags;
  static getFileNotes = storage.getFileNotes;
  static addFileNote = storage.addFileNote;
  static updateFileNote = storage.updateFileNote;
  static deleteFileNote = storage.deleteFileNote;
  static getAllNotes = storage.getAllNotes;
  static searchNotes = storage.searchNotes;
  static batchSetNotes = storage.batchSetNotes;
  static getFileAnnotations = storage.getFileAnnotations;
  static addFileAnnotation = storage.addFileAnnotation;
  static toggleAnnotationResolved = storage.toggleAnnotationResolved;
  static deleteFileAnnotation = storage.deleteFileAnnotation;
  static getAllAnnotations = storage.getAllAnnotations;
  static getTagCategories = storage.getTagCategories;
  static addTagCategory = storage.addTagCategory;
  static updateTagCategory = storage.updateTagCategory;
  static deleteTagCategory = storage.deleteTagCategory;
  static getFileMetadata = storage.getFileMetadata;
  static setFileMetadata = storage.setFileMetadata;
  static getAllMetadataKeys = storage.getAllMetadataKeys;
  static getChatSessions = storage.getChatSessions;
  static getChatSession = storage.getChatSession;
  static saveChatSession = storage.saveChatSession;
  static deleteChatSession = storage.deleteChatSession;
  static clearChatHistory = storage.clearChatHistory;
  static getChatsDirectory = storage.getChatsDirectory;
  static createChatFile = storage.createChatFile;
  static readChatFile = storage.readChatFile;
  static saveChatFile = storage.saveChatFile;
  static getChatFileSummary = storage.getChatFileSummary;
  static listSqliteTables = storage.listSqliteTables;
  static getSqliteTableColumns = storage.getSqliteTableColumns;
  static querySqliteTable = storage.querySqliteTable;
  static executeSqliteQuery = storage.executeSqliteQuery;

  // ── PTY (interactive terminal) ──────────────────────────────────────────
  static ptySpawn = pty.ptySpawn;
  static ptyWrite = pty.ptyWrite;
  static ptyResize = pty.ptyResize;
  static ptyKill = pty.ptyKill;
  static ptyKillAll = pty.ptyKillAll;
  static listenToPtyOutput = pty.listenToPtyOutput;
  static listenToPtyExit = pty.listenToPtyExit;

  // ── System ──────────────────────────────────────────────────────────────
  static executeCommand = system.executeCommand;
  static executeCommandStream = system.executeCommandStream;
  static getCurrentShell = system.getCurrentShell;
  static openInTerminal = system.openInTerminal;
  static listenToTerminalOutput = system.listenToTerminalOutput;
  static listenToEvent = system.listenToEvent;
  static getAppVersion = system.getAppVersion;
  static getSystemInfo = system.getSystemInfo;
  static getUserDirectories = system.getUserDirectories;
  static getRecentFolders = system.getRecentFolders;
  static addToRecentFolders = system.addToRecentFolders;
  static listDrives = system.listDrives;
  static ejectVolume = system.ejectVolume;
  static getShortcuts = system.getShortcuts;
  static getShortcutsByCategory = system.getShortcutsByCategory;
  static addShortcut = system.addShortcut;
  static updateShortcut = system.updateShortcut;
  static removeShortcut = system.removeShortcut;
  static getShortcutProfiles = system.getShortcutProfiles;
  static switchShortcutProfile = system.switchShortcutProfile;
  static getShortcutSettings = system.getShortcutSettings;
  static updateShortcutSettings = system.updateShortcutSettings;
  static executeShortcutAction = system.executeShortcutAction;
  static registerExtensionShortcut = system.registerExtensionShortcut;
  static unregisterExtensionShortcuts = system.unregisterExtensionShortcuts;
  static registerGlobalShortcuts = system.registerGlobalShortcuts;
  static unregisterGlobalShortcuts = system.unregisterGlobalShortcuts;
  static toggleGlobalShortcuts = system.toggleGlobalShortcuts;
  static resetShortcuts = system.resetShortcuts;
  static resetSingleShortcut = system.resetSingleShortcut;
  static createBackup = system.createBackup;
  static listBackups = system.listBackups;
  static restoreBackup = system.restoreBackup;
  static deleteBackup = system.deleteBackup;
  static getAuditLog = system.getAuditLog;
  static clearAuditLog = system.clearAuditLog;
  static exportAuditLog = system.exportAuditLog;
  static dockerIsAvailable = system.dockerIsAvailable;
  static dockerListContainers = system.dockerListContainers;
  static dockerListImages = system.dockerListImages;
  static dockerStartContainer = system.dockerStartContainer;
  static dockerStopContainer = system.dockerStopContainer;
  static dockerRemoveContainer = system.dockerRemoveContainer;
  static dockerRemoveImage = system.dockerRemoveImage;
  static dockerContainerLogs = system.dockerContainerLogs;
  static enableVersioning = system.enableVersioning;
  static disableVersioning = system.disableVersioning;
  static createVersion = system.createVersion;
  static listVersions = system.listVersions;
  static restoreVersion = system.restoreVersion;
  static deleteVersion = system.deleteVersion;
  static getVersioningConfig = system.getVersioningConfig;
  static updateVersioningConfig = system.updateVersioningConfig;
  static isVersioningEnabled = system.isVersioningEnabled;
  static getVersionCount = system.getVersionCount;
  static deleteAllVersions = system.deleteAllVersions;
  static readVersionContent = system.readVersionContent;
  static syncBookmarksToCloud = system.syncBookmarksToCloud;
  static fetchCloudBookmarks = system.fetchCloudBookmarks;
  static setLastSyncTime = system.setLastSyncTime;
  static getLastSyncTime = system.getLastSyncTime;
  static syncAll = system.syncAll;
  static startAutoSync = system.startAutoSync;
  static stopAutoSync = system.stopAutoSync;
  static getAutoSyncStatus = system.getAutoSyncStatus;
  static setDefaultFolderHandler = system.setDefaultFolderHandler;
  static addContextMenuEntry = system.addContextMenuEntry;
  static removeContextMenuEntry = system.removeContextMenuEntry;
  static getShellIntegrationStatus = system.getShellIntegrationStatus;
  static gdriveAuthenticate = system.gdriveAuthenticate;
  static gdriveDisconnect = system.gdriveDisconnect;
  static gdriveListAccounts = system.gdriveListAccounts;
  static gdriveListFiles = system.gdriveListFiles;
  static gdriveDownloadFile = system.gdriveDownloadFile;
  static gdriveUploadFile = system.gdriveUploadFile;
  static gdriveDeleteFile = system.gdriveDeleteFile;
  static gdriveRenameFile = system.gdriveRenameFile;
  static gdriveMoveFile = system.gdriveMoveFile;
  static gdriveCreateFolder = system.gdriveCreateFolder;
  static gdriveGetFileContent = system.gdriveGetFileContent;
  static getGdriveSettings = system.getGdriveSettings;
  static updateGdriveSettings = system.updateGdriveSettings;
}
